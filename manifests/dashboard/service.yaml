apiVersion: v1
kind: Service
metadata:
  name: katib-results-dashboard
  namespace: mlops-apps
spec:
  selector:
    app: katib-results-dashboard
  ports:
    - name: http
      port: 80
      targetPort: 8000
